Paleta Marinho & Coral da SAL. Regras duras: fundo sempre pela escada `--bg → --bg-2 → --surface → --surface-2`; borda é sempre `var(--line)` (uma só); coral (`--gold`) é o ÚNICO acento e cobre no máximo ~5% da tela (botões primários, links de ação, grãos de sal); `--lima` (#F6FF74) só como marca-texto (classe `.grifo`) e em peças de marca — nunca em botões; `--violet` é papel de suporte (hover, foco). Tema claro: trocar `data-tema="claro"` no html — os tokens se ajustam sozinhos.
